import * as React from 'react';
import PageContainer from '../../PageContainer.css';

const AdminDashboard: React.FC = () => (
  <PageContainer>
    <main>
      List of Admin editing options
    </main>
  </PageContainer>
);
export default AdminDashboard;
