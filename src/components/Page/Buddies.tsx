import * as React from 'react';
import PageContainer from "../PageContainer.css";

const Buddies: React.FC = () => {
	return (
		<PageContainer>
			<main>
				'No buddies yet' + ADD or some profile pics with names (grid) -<br/>
				Once you click on a buddy you can either see your challenge board with them, or some stats
			</main>
		</PageContainer>
	);
}
export default Buddies;
